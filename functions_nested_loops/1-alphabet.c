#include "main.h"
/**
 *print_alphabet - affiche l'alphabet
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
char c
for (c = 'a' ; c <= 'z' ; c++)
_putchar(char c);
_putchar('\n');
}
return (0);
}
