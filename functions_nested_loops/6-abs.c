#include "main.h"
#include <ctype.h>
/**
 * _abs - computes absolute value of a number
 * @n: number to check
 * Return: n if n greater than 0, return -n if less than 0
 */
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else
{
return (-n);
}
}
