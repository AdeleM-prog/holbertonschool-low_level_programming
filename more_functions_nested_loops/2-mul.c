#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: int to multiply
 * @b: int to multiply
 * Return: always 0
 */
int mul(int a, int b)
{
int x;
a *b = x;
return (0);
}
