#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length of a string.
 *
 * @s: pointed variable to check
 * Return: always 0
 */
int _strlen(char *s)
{
int length;
length = strlen(s);
return (length);
}
