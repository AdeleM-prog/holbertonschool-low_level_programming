#include "main.h"
/**
 * swap_int - swaps the value of two integers
 * @a: int to swap
 * @b: int to swap
 * Return: void
 */
void swap_int(int *a, int *b)
{
int temp = 0;
int a;
int b;
*a = temp;
*a = *b;
*b = temp;
}
