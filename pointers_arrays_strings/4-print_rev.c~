#include "main.h"
/**
 * _puts - prints a string
 * @str: string to print
 * Return: void
 */
void print_rev(char *s)
{
int i;
for (i = _strlen(s); s[i] >= 0 ; i--)
{
_putchar([i]);
}
_putchar('\n');
}
