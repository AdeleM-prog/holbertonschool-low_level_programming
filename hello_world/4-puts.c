#include <stdio.h>
/**C program displaying a string
*main: main function of the program, returning int
*return (0): everything worked correctly !**/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
